---
"mattpocock-skills": minor
---

Add the model-invoked `choosing-clear-identifiers` and `structuring-code-modules` engineering skills. The first provides point-of-use naming rules and a four-way audit classification. The second applies a package gate to file and module restructuring while using `codebase-design` as its vocabulary layer.
