---
"mattpocock-skills": patch
---

Make `implement` load the personal naming and module-structure standards and use Ponytail when available. Extend `code-review` so its Standards sub-agent always applies those standards, while an available `ponytail-review` runs as a separate optional perspective.
